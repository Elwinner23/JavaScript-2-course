let numbers = [25, 4, 18, 151, 5, 15];
for(let i = 0; i < numbers.length; i++)
{
    console.log(number[i]);
}
let i = 0;
let sum = 0;
while(i < numbers.length)
{
    sum += numbers[i];
}
console.log(sum);